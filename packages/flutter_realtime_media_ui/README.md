# flutter_realtime_media_ui

Provider-neutral, ready-to-use meeting UI. The repository Demo consumes this
same package, so the default SDK experience and Demo stay aligned.

```dart
MediaRoomView(room: room, renderer: renderer)
```
