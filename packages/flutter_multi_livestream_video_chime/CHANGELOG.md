# Changelog

## 0.1.0

- Initial provider-neutral adapter around `flutter_aws_chime` v3.
- Participant lifecycle, media controls, devices, data messages, events, video
  tile rendering, and typed Core error mapping.
